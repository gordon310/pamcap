# 酒店资产估值验证应用（专家版）开发需求

| 项 | 内容 |
|---|---|
| 文档名称 | 酒店资产估值验证应用（专家版）开发需求 |
| 版本 | V1.0 |
| 日期 | 2026-09-20 |
| 目标读者 | 开发 Agent / 前端工程、算法工程 |
| 关联文档 | 《“螃蟹树聚”产品需求文档（PRD）V3.5》4.7–4.12、《SRS V1.4》FR-CLS/FR-VAL/FR-FNB、《WBS V1.4》11.9/11.10 |
| 交付形态 | 纯前端 SPA（React + TypeScript），零后端；内置行业基准库；专家可调系数、录入意见、版本留痕；定稿后导出为可供平台调用的 **Skill 包**（标准 JSON Schema + 计算逻辑） |

---

## 1. 背景与目标

### 1.1 背景
一期以"业主单位"采集最基本信息（房间数强制、其他收入默认 0、详细字段保留后补）。在数据尚不充分阶段，需要用**行业基准估算**生成估值区间，并由专家验证估值模型的**可行性**与**计算过程**，最终把经过验证、系数定稿的估值逻辑沉淀为平台 Skill。

### 1.2 目标（一期）
1. 专家录入酒店基本信息 → 应用**自动生成** RevPAR / GOP / NOI / EBITDA / Cap Rate / IRR / 估值区间。
2. **完整呈现计算过程与逻辑**：每步公式、代入值、结果、数据来源、置信度。
3. **专家可纠偏调整系数**（不是直接改结论）：调整后自动重算。
4. 专家可**录入意见**（绑定到步骤/参数/结论）。
5. 保存**版本 + 留痕**；**定稿**后导出 **Skill 包**供平台系统调用。

### 1.3 非目标（本期不做）
- 不做平台系统集成（登录/权限/数据库/向量库）；数据来源为内置基准库 + 手工/模板输入。
- 不做对话式智能体（资小蟹、二期）。
- 不做正式对外估值报告（须持证评估师复核）。

### 1.4 成功判据
专家可在 10 分钟内完成一次输入 → 查看完整计算轨迹 → 调整若干系数 → 复算 → 录入意见 → 定稿 → 导出 Skill 包；且对同一输入，复算结果可复现（确定性）。

---

## 2. 角色与使用模式

| 角色 | 说明 |
|---|---|
| 专家（唯一使用者） | 平台雇员/合伙人；录入、调参、评注、定稿 |
| 平台系统（Skill 消费方） | 通过标准 JSON Schema 调用定稿 Skill |

模式：**单专家 + 版本留痕**。每次"调参/意见/定稿"生成版本快照，可回溯对比。

---

## 3. 总体设计

- **技术栈**：React 18 + TypeScript + Vite；状态管理（Zustand 或 Redux Toolkit）；UI 组件库（Ant Design 或 Mantine）；图表（ECharts 或 Recharts）；本地存储（IndexedDB / localStorage）；无后端。
- **计算**：全部在前端本地执行；计算引擎为**纯函数、确定性、无副作用**，便于测试与 Skill 化。
- **基准库**：内置 `baseline.json`（见 §5），随应用打包；专家调整覆盖值（overrides）与基准分离存储。
- **可移植**：计算引擎不得依赖 DOM；导出为独立 TS 模块（`valuation-engine`），平台可复用或按伪代码转实现。
- **语言**：中文 UI；金额单位**万元**（可切换元）；面积单位 ㎡。

```
┌─────────────────────────────┐
│  输入表单 Input            │
├─────────────────────────────┤
│  分类解析 Classification   │  ← 名称映射表 + 手工校准
├─────────────────────────────┤
│  基准库 Baseline (JSON)    │  ← 内置；专家 overrides
├─────────────────────────────┤
│  计算引擎 Engine (纯函数)   │  → Trace（逐步过程）
├─────────────────────────────┤
│  结果视图 + 过程视图        │
├─────────────────────────────┤
│  专家纠偏 / 意见 / 版本     │
├─────────────────────────────┤
│  定稿 → 导出 Skill 包       │
└─────────────────────────────┘
```

---

## 4. 输入模型（一期必填最小集）

> 字段以 JSON 定义（见 §11.1）。新建/存量共用字段；投资口径二选一。

### 4.1 通用（必填，标 ★）与选填
| 字段 | 键名 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| 酒店名称 | `hotel_name` | string | ★ | 用于自动分类；可手工校准 |
| 房间数 | `rooms` | integer | ★ | 强制；>0 |
| 开业时间 | `opening_date` | date | ★ | 经营年限/剩余经营期 |
| 总建筑面积（含地下室） | `gfa` | number(㎡) | ★ | 坪效、单方造价 |
| 城市等级 | `city_tier` | enum | ★ | 一线/新一线/二线/三线/四线及以下（可由名称映射） |
| 区位 | `location` | enum | ★ | CBD/次中心/近郊新区/景区度假地/机场高铁/产业园区/其他 |
| 档次 | `segment` | enum | ★ | S1–S6 |
| 业态 | `property_type` | enum | ★ | P1–P7 |
| 经营模式 | `operation_mode` | enum | ★ | M1–M4（M5 混合已取消） |
| 携程均价 | `ctrip_adr` | number(元) | ★ | 含税；应用内 ×0.7 折 |
| 全年出租率（业主填报） | `occupancy_input` | number(0–1) | ★ | 业主易高估，应用内 ×校正系数 |
| 客房餐饮收入比 | `fb_ratio` | number | ★ | 餐饮收入 ÷ 客房收入（默认口径；UI 内注明，专家可切换为"占客房比"或其他解释） |
| 业主利润（EBITDA 口径） | `owner_ebitda` | number(万元) | ★ | 作为校准锚点（可为空则仅用估算） |
| 其他收入 | `other_income` | number(万元) | 选填 | 默认 0 |

### 4.2 投资与资本
| 字段 | 键名 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| 投资口径 | `capex_type` | enum | ★ | `new`（新建）/ `acquisition`（存量购买）/ `self_renew`（自有更新） |
| 建造成本（不含土地，开业前） | `construction_cost` | number(万元) | ★（new） | 总金额 |
| 存量购买价 | `acquisition_price` | number(万元) | ★（acquisition） | |
| 原建造成本 | `original_cost` | number(万元) | ★（self_renew） | |
| 改造费用 | `renovation_cost` | number(万元) | ★（self_renew） | |
| 自有资金 | `equity` | number(万元) | ★ | IRR 杠杆基数；0<equity≤总投资 |

### 4.3 校验规则
- 数值字段 > 0（其他收入 ≥ 0）；出租率 0–1；`fb_ratio` ≥ 0。
- 总投资 = `construction_cost` / `acquisition_price` / (`original_cost`+`renovation_cost`)。
- `equity` ≤ 总投资；否则报错并提示。
- 分类字段若由名称自动解析失败 → 提示手工选择，不阻断。
- 所有缺失的选填字段按基准默认并在过程视图中标注"默认/估算"。

---

## 5. 行业基准库（Baseline）

内置 `baseline.json`，专家可覆盖（overrides），二者分离。示例结构：

```json
{
  "meta": { "version": "1.0", "updated": "2026-09-20", "source": "平台智库/〔HOST〕/可比交易/GB标准（示例）" },
  "adr": { "ctrip_discount": 0.7 },
  "occupancy": { "bias_correction": 0.95 },
  "revenue": { "days_per_year": 365, "other_income_default": 0.0 },
  "gop_rate": {
    "by_segment": { "S1": 0.30, "S2": 0.28, "S3": 0.26, "S4": 0.24, "S5": 0.22, "S6": 0.20 },
    "by_property_type_adj": { "P1": -0.03, "P2": -0.01, "P3": 0.02, "P4": -0.01, "P5": -0.02, "P6": 0.03, "P7": -0.02 }
  },
  "cost_rates": {
    "management_fee_rate": 0.03,
    "property_tax_rate": 0.012,
    "insurance_rate": 0.003,
    "ffe_rate": 0.04
  },
  "owner_expense_coef": { "by_segment": { "S1": 0.03, "S2": 0.03, "S3": 0.02, "S4": 0.02, "S5": 0.015, "S6": 0.015 } },
  "cap_rate_base": {
    "by_segment": { "S1": 0.045, "S2": 0.05, "S3": 0.055, "S4": 0.06, "S5": 0.065, "S6": 0.07 }
  },
  "ebitda_multiple": {
    "by_segment": { "S1": [22, 30], "S2": [20, 28], "S3": [18, 25], "S4": [16, 22], "S5": [14, 20], "S6": [12, 18] }
  },
  "location_coefficient": {
    "note": "城市等级 × 区位 → 地段系数（>1 越优）；依据 GB/T 18507-2014、GB/T 18508-2014",
    "table": {
      "一线":     { "CBD": 1.15, "次中心": 1.08, "近郊新区": 0.98, "景区度假地": 1.05, "机场高铁": 1.00, "产业园区": 0.95, "其他": 1.00 },
      "新一线":   { "CBD": 1.12, "次中心": 1.05, "近郊新区": 0.96, "景区度假地": 1.03, "机场高铁": 0.98, "产业园区": 0.93, "其他": 0.98 },
      "二线":     { "CBD": 1.08, "次中心": 1.02, "近郊新区": 0.94, "景区度假地": 1.01, "机场高铁": 0.96, "产业园区": 0.90, "其他": 0.95 },
      "三线":     { "CBD": 1.03, "次中心": 0.98, "近郊新区": 0.90, "景区度假地": 0.98, "机场高铁": 0.92, "产业园区": 0.86, "其他": 0.90 },
      "四线及以下": { "CBD": 0.98, "次中心": 0.93, "近郊新区": 0.85, "景区度假地": 0.95, "机场高铁": 0.88, "产业园区": 0.82, "其他": 0.85 }
    }
  },
  "financing": { "interest_rate": 0.045, "loan_tenor_years": 10, "holding_period_years": 10 },
  "cost_approach": { "land_benchmark_price_per_sqm": 0.0, "depreciation_rate": 0.0 },
  "scenario": { "adr_delta": 0.05, "occ_delta": 0.03, "cap_delta_bps": 25, "multiple_delta": 2 }
}
```

> 基准为**示例值**，须由专家校准；应用需支持导入/导出基准库 JSON，以及"恢复默认"。

---

## 6. 计算引擎（完整计算链）

### 6.1 计算步骤（每步产出 Trace 记录）
| 步 | 名称 | 公式 | 来源 |
|---|---|---|---|
| S0 | 分类解析 | 由 `hotel_name` 映射（或手工）得 segment/property_type/city_tier/location/operation_mode | 映射表/专家 |
| S1 | ADR | `adr = ctrip_adr × ctrip_discount` | 携程 × 基准折扣 |
| S2 | OCC | `occ = occupancy_input × occupancy.bias_correction` | 输入 × 校正 |
| S3 | RevPAR | `revpar = adr × occ` | 计算 |
| S4 | 客房收入 | `room_rev = revpar × rooms × days_per_year`（万元换算） | 计算 |
| S5 | 餐饮收入 | `fb_rev = room_rev × fb_ratio` | 输入比 |
| S6 | GOR | `gor = room_rev + fb_rev + other_income` | 计算 |
| S7 | GOP | `gop = gor × gop_rate`；`gop_rate = by_segment[segment] + by_property_type_adj[property_type]` | 基准 |
| S8 | FF&E | `ffe = gor × ffe_rate` | 基准 I402 |
| S9 | 管理费 | `mgmt_fee = gor × management_fee_rate` | 基准 R118 |
| S10 | 物业税 | `prop_tax = gor × property_tax_rate` | 基准 |
| S11 | 保险 | `insurance = gor × insurance_rate` | 基准 |
| S12 | NOI | `noi = gop − mgmt_fee − prop_tax − insurance − ffe` | 计算 |
| S13 | 业主费用 | `owner_exp = gor × owner_expense_coef[segment]` | 基准 |
| S14 | EBITDA（估算） | `ebitda_est = noi − owner_exp` | 计算 |
| S15 | 校准 | `delta = owner_ebitda − ebitda_est`；展示差异率；专家选择采用"估算/输入/加权" | 输入对照 |
| S16 | 地段系数 | `loc_coef = location_coefficient.table[city_tier][location]` | GB/T 18507/18508 |
| S17 | Cap Rate | `cap = cap_rate_base[segment] × loc_coef` | 基准 × 地段 |
| S18 | 收益法估值 | `v_income = noi / cap` | 计算 |
| S19 | 倍数法估值 | `v_multiple = ebitda × multiple`（multiple 取 `ebitda_multiple[segment]` 中值或专家值） | 基准 |
| S20 | 成本法下限 | `v_cost = 总投资 + 土地价值 − 贬值`；`土地价值 = land_benchmark_price_per_sqm × land_area × loc_coef`（土地面积缺省取 GFA） | GB/T 18508 |
| S21 | 估值区间 | 保守/基准/乐观：对 ADR、OCC、Cap、multiple 施加 `scenario` 偏移后重算 | 计算 |
| S22 | DSCR | `dscr = noi / 年还本付息`；贷款 = 总投资 − equity | 计算 |
| S23 | IRR | 以 equity 为初始投入，逐年 `noi`（可增长 0），末年退出 `ebitda_terminal × multiple`；解 `Σ CFt/(1+irr)^t = 0` | 计算 |

> 单位：内部统一"万元"，展示可切换；金额换算 `元→万元` 除以 10000。

### 6.2 输出结果
- RevPAR、ADR、OCC、GOR、GOP（含 GOP 率）、NOI、EBITDA（估算/输入/采用值）、Cap Rate、地段系数、DSCR、IRR。
- 估值：收益法、倍数法、成本法下限、**估值区间（保守/基准/乐观）**、每间房价值、每㎡价值。
- 对标占位（可选，需基准）。

### 6.3 确定性与精度
- 纯函数；相同输入 + 相同基准 + 相同 overrides ⇒ 相同输出。
- 金额保留 2 位、比率保留 4 位（展示可格式化）；IRR 用二分法/牛顿法求解，容差 1e-6。

---

## 7. 计算过程与逻辑呈现（核心）

### 7.1 过程视图
以表格/时间线逐步展示（对应 §6.1）：
| 步 | 名称 | 公式 | 代入 | 结果 | 来源 | 备注 |
|---|---|---|---|---|---|---|
| S3 | RevPAR | `adr × occ` | `1000 × 0.684` | `684 元` | 计算 | 出租率已校正 |

- 每步可展开显示原始定义与出处（携程/〔HOST〕/〔智库〕/GB/T/专家调整）。
- 被专家调整过的系数以高亮 + 角标（含调整理由）显示。
- 估算值统一打【估算】标记；默认值打【默认】标记。

### 7.2 逻辑呈现要求
- 提供"公式字典"页，列出所有公式、变量定义、单位与来源。
- 提供"样例演算"（用当前输入逐行代入）。
- 提供"敏感度"：ADR/OCC/Cap/倍数 单因素 ±变动对估值的影响（表/图）。

---

## 8. 专家纠偏（只调系数）

### 8.1 可调系数清单
ADR 折扣、OCC 校正、GOP 率（含业态调整）、FF&E 率、管理费率、物业税率、保险率、业主费用系数、地段系数（表项）、Cap 基准、EBITDA 倍数、融资参数（利率/期限/持有期）、情景偏移。

### 8.2 规则
- 专家**不得直接修改结论数值**，只能改系数；修改后系统**自动重算**全部下游结果。
- 每次修改生成**留痕**：`{who, at, key, old_value, new_value, reason}`；并生成**新版本**。
- 支持"恢复默认""重置某步"。
- 调整影响范围提示（例如改 Cap 会影响收益法估值与 IRR 退出）。

---

## 9. 专家意见

- 意见可绑定到：具体步骤（如 S7 GOP 率）、具体系数、或整体结论。
- 字段：`{id, target, content, author, at, status(草稿/采纳/驳回)}`。
- 意见区支持富文本/条目化；定稿时导出到报告与 Skill 包。

---

## 10. 版本、定稿与 Skill 化

### 10.1 版本
每次"保存/调参/加意见"生成版本快照（输入 + overrides + 意见 + 结果 + trace）。支持版本列表、查看、**对比（diff）**、回滚。

### 10.2 定稿
专家点击"定稿"：锁定当前 overrides 为**定稿系数**，标记版本为 `final`。

### 10.3 Skill 包导出
导出结构（zip 或目录）：
```
valuation-skill/
├── skill.json          # 元数据 + 输入/输出 JSON Schema + 版本
├── baseline.json       # 定稿基准与 overrides 合并后的系数
├── engine/             # 计算引擎（TS 源码 + 编译产物；纯函数）
├── examples/           # 示例输入/输出（含 trace）
└── README.md           # 调用说明与公式字典
```
`skill.json` 定义平台调用契约（语言无关）：
- 输入：`ValuationInput`（§11.1）
- 输出：`ValuationResult`（§11.4，含 `trace`）
- 调用：`valuate(input, baseline) -> result`；平台可用任意语言按伪代码/公式实现，或复用 TS 模块。

---

## 11. 数据模型（JSON Schema 摘要）

### 11.1 ValuationInput
```json
{
  "hotel_name": "华东_高端_003",
  "rooms": 320,
  "opening_date": "2018-06-01",
  "gfa": 42000,
  "classification": { "segment": "S3", "property_type": "P1", "city_tier": "一线", "location": "CBD", "operation_mode": "M1" },
  "ctrip_adr": 1250,
  "occupancy_input": 0.72,
  "fb_ratio": 0.35,
  "owner_ebitda": 3200,
  "other_income": 0,
  "investment": { "capex_type": "new", "construction_cost": 86000, "equity": 38000 }
}
```

### 11.2 Baseline / Overrides
见 §5；`overrides` 为 `{key_path: value}` 的稀疏映射 + 每次调整的留痕。

### 11.3 TraceItem
```json
{ "step": "S3", "name": "RevPAR", "formula": "adr × occ",
  "inputs": { "adr": 875, "occ": 0.684 }, "result": 598.5, "unit": "元",
  "source": "计算", "flags": ["estimated"], "adjusted": false, "note": "" }
```

### 11.4 ValuationResult
```json
{
  "metrics": { "adr": 875, "occ": 0.684, "revpar": 598.5, "gor": 8100, "gop": 2106,
               "noi": 1650, "ebitda_est": 1490, "owner_ebitda": 3200, "ebitda_used": 1490,
               "cap": 0.0632, "location_coef": 1.15, "dscr": 1.4, "irr": 0.118 },
  "valuation": { "income": 26108, "multiple": 29800, "cost_floor": 98000,
                 "range": { "conservative": 23500, "base": 26108, "optimistic": 29500 },
                 "per_room": 81.6, "per_sqm": 0.62, "unit": "万元" },
  "trace": [ /* TraceItem[] */ ],
  "warnings": [ /* 数据缺失、口径提示 */ ]
}
```

### 11.5 Opinion / Adjustment / Version
```json
{ "opinions": [ { "id": "OP1", "target": "S7", "content": "GOP率偏高", "author": "expert", "at": "2026-09-20T10:00:00Z", "status": "采纳" } ],
  "adjustments": [ { "who": "expert", "at": "...", "key": "gop_rate.by_segment.S3", "old": 0.26, "new": 0.24, "reason": "餐饮占比高" } ],
  "versions": [ { "id": "v1", "status": "final", "at": "..." } ] }
```

---

## 12. 页面与交互

| 页面 | 内容 |
|---|---|
| 1 输入页 | 表单（§4）；名称→分类自动带出可改；模板导入/导出；校验提示 |
| 2 结果页 | 指标卡 + 估值区间 + 图表（瀑布/敏感度） |
| 3 过程页 | 计算轨迹表（§7），可展开公式与来源；高亮调整项 |
| 4 系数页 | 基准库与 overrides 编辑器；恢复默认；影响范围提示 |
| 5 意见页 | 意见列表、绑定目标、状态 |
| 6 版本页 | 版本列表、对比、回滚、定稿 |
| 7 导出页 | 导出 Skill 包 / 结果 JSON / 报告（可打印 PDF） |

交互要求：任何系数调整后**实时**重算并刷新结果与过程视图；顶部固定显示"当前版本 / 是否已定稿 / 差异提示"。

---

## 13. 非功能需求
- 性能：单次完整计算（含 3 情景 + IRR）< 200ms；录入到出结果即时。
- 可离线：纯前端，无网络可用（除可选导入文件）。
- 兼容：Chrome/Edge 近两年版本。
- 可测试：引擎单元测试覆盖 §6.1 每步与边界；提供黄金样例（输入→期望输出）。
- 可维护：基准与逻辑分离；公式集中在 `formulas.ts`；类型完备（TS strict）。
- 数据安全：数据仅在浏览器本地；导出文件由专家自行保管。

---

## 14. 验收标准
1. 输入 §4 必填项后，输出 §6.2 全部结果，且过程页展示 §6.1 每一步（公式/代入/结果/来源）。
2. 修改任一可调系数 → 结果与过程视图自动更新，且生成留痕与新版本。
3. 意见可绑定步骤/系数/结论并保存。
4. 定稿后可导出符合 §10.3 的 Skill 包，`skill.json` 内 Schema 完整。
5. 相同输入+基准+overrides 重复计算，结果完全一致（确定性）。
6. 缺失选填字段时，按默认值计算并在过程视图标注【默认】；数值异常被校验拦截。
7. 提供至少 2 个黄金样例（新建 / 存量改造），断言通过。

---

## 15. 交付物
1. 可运行的纯前端应用（源码 + 构建产物）。
2. 计算引擎独立模块 + 单元测试 + 黄金样例。
3. `baseline.json`（示例基准）与导入导出。
4. 定稿后导出的 `valuation-skill` 包（含 `skill.json` Schema 与文档）。
5. README（运行、公式字典、Skill 调用说明）。

---

## 16. 附录
### 16.1 关键口径备忘
- 业主利润 = **EBITDA 口径**；`NOI = EBITDA + 业主费用`，应用内以 `owner_expense_coef` 桥接，专家可调。
- GOP 为**行业率估算**，仅客房＋餐饮（其他收入默认 0）。
- Cap Rate = **分类基准 × 地段系数**；地段系数依据 **GB/T 18507-2014 / GB/T 18508-2014**。
- 估值 = 收益法（NOI÷Cap）为主，EBITDA 倍数（20–30，按分类）交叉，成本法（含土地）下限；输出区间。
- 出租率须经校正；ADR 用携程均价 ×0.7。
- 定稿 Skill 仅用于**内部预估/验证**，对外正式报告仍须持证评估师复核。
